// Quick-cash tender buttons on the checkout screen — a genuine static UI
// constant, not backend data. Menu items, categories, modifier groups,
// discounts, and the order queue are all live-fetched from Supabase now
// (see useCremaPos.ts).
export const QUICK_CASH = [100, 200, 500, 1000];
